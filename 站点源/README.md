# 站点源(已弃用)

`生成站点.qi` 是早期用奇语言写的静态站点生成器(dogfooding),曾负责渲染 `../docs/` 与 `../blog/` 的旧版页面(宣纸底 + 朱砂主题,样式内联在每页)。

**2026-07-03 起已弃用**:官网改为直接维护静态 HTML,主题收敛到共享的 `/site.css` + `/theme.js`(dark 默认 + light 切换,与新首页同一设计语言)。本目录仅作历史保留,请勿再运行它生成页面——输出会覆盖新壳。

改文档/博客请直接编辑 `../docs/*.html` 与 `../blog/*.html`。
